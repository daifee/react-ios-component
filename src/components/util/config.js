/**
 * @author haw
 */

/**
 * 项目默认的配置参数
 * @type {Object}  数据配置对象
 * @property {String}  config.namespace='rc-ios'  css 默认的命名空间
 */
const config = {
  namespace: 'rc-ios-',
  colors: [
    '',
    'blue',
    'red',
    'black',
    'lightblue',
    'yellow',
    'orange',
    'pink',
    'green',
    'gray'
  ]
};

export default config;
